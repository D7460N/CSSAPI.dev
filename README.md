# CSSAPI.dev
CSSAPI is a declarative HTML/CSS UI API. UI features are exposed as drop-in semantic components: you provide structure and data, and CSS handles all state and behavior—no framework, no JS UI logic.
